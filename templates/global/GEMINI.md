# Global Rules

- Maintain efficiency (concise responses, no full code repeats).
- Follow agent identity guidelines.
- Ask for clarification instead of guessing.
- **CRITICAL**: ALWAYS ask the user for explicit permission BEFORE running `git commit`, `git push`, or `sudo` commands using terminal tools. Do not run them arbitrarily.
